Ошибки были в классе Lorry:
1. Наследование от Car, но имплементация от Moveable и Stopable,
нельзя рдительские классы и интерфейсы смешивать в extends.
2. В этом же классе забыли сделать переопределение абстрактного метода open(),
а так же забыли надписать аннотацию @Override над переопределямыми методами от интерфейсов.
